
/**
 * @file mirrors.h
 * @brief Contains documentation elements.
 */

/**
 * @namespace mirrors
 * @brief Main namespace of the Mirrors Server.
 */

/**
 * @mainpage
 *
 * This is a server application for providing marker detection to the Mirrors AR game.
 *
 * This server provides relative positions and rotations of markers to the
 * Unity-based client applications. The application contains two main components:
 *
 * @li The detector class, which provides marker detection functionality
 * @li The ServerSocket class, which maintains socket connections to clients.
 *
 * The application makes use of the NetLink library for managing socket connections.
 * Documentation for this library is not included.
 */
