First line contains a single number showing the width of the level (in marker sizes)
second line contains a single number showing thheight of the level 
after that a line containing a set of cells equal to width where each line is followed with a empty line.
Each cell contains a type (w for wall . for empty e for emiter t for target) a number (degrees in rotation divided by 45 with right being 0) and then a space.
